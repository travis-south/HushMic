[package]
name = "dpdfnet-ladspa"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]   # cdylib for the LADSPA .so; rlib for the test suites

[dependencies]
ladspa = "0.3.4"
hushmic-denoiser = { path = "../hushmic-denoiser", version = "0.9.1" }
rtrb = "0.4.0"
libc = "0.2"

[dev-dependencies]
claxon = "0.4" # FLAC decode for the committed public parity fixture

[features]
experimental-openvino = ["hushmic-denoiser/experimental-openvino"]
